{
  "name": "engineering-portfolio-platform",
  "version": "1.0.0",
  "private": true,
  "description": "Engineering Portfolio & AI Career Platform by Guilherme Rodovalho",
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --recursive run build",
    "typecheck": "pnpm --recursive run typecheck",
    "lint": "pnpm --recursive run lint",
    "test": "vitest run",
    "clean": "pnpm --recursive run clean"
  },
  "packageManager": "pnpm@9.12.1",
  "engines": {
    "node": ">=20.0.0"
  },
  "devDependencies": {
    "pdf-lib": "^1.17.1",
    "prettier": "^3.3.3",
    "typescript": "^5.6.3",
    "vitest": "^2.1.9"
  }
}
